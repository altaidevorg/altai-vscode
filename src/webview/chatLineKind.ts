/**
 * Classify TASK-009 flat log lines for Desktop-like bubble styling.
 * Shared implementation lives in `@altai/agent-ui` (A6.96).
 */

export {
  chatLineKind,
  shouldShowFlatLogEmptyHome as shouldShowChatEmptyHome,
  type ChatLineKind,
} from "@altai/agent-ui";
