/**
 * Pure formatter: export Chat transcript for clipboard paste.
 * Shared implementation lives in `@altai/agent-ui` (A6.85).
 */

export {
  formatTranscriptForCopy,
  roleLabelForCopy,
  type TranscriptCopyLine,
} from "@altai/agent-ui";
