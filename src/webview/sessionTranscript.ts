/**
 * Pure helpers for formatting session transcripts into the TASK-009 chat log.
 * Shared implementation lives in `@altai/agent-ui` (A6.103).
 */

export {
  formatSessionMessageLine,
  transcriptLinesFromMessages,
  type TranscriptMessage,
} from "@altai/agent-ui";
