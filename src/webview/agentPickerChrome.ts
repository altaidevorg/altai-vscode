/**
 * Built-in ALTAI agent profiles for the VS Code composer switcher.
 * Shared implementation lives in `@altai/agent-ui` (A6.88).
 */

export {
  COMPOSER_AGENT_PROFILES,
  DEFAULT_COMPOSER_AGENT_ID,
  applyAgentPromptPrefix,
  canMountAgentPicker,
  resolveComposerAgent,
  type ComposerAgentIconId,
  type ComposerAgentProfile,
} from "@altai/agent-ui";
