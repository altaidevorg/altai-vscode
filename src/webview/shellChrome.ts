/**
 * Pure helpers for the VS Code side-chat shell chrome (Desktop AiSidePanel density).
 * Shared implementation lives in `@altai/agent-ui` (A6.74).
 */

export {
  compactHostStatusLabel,
  nextSurfaceAfterSettingsToggle,
  settingsGearPressed,
  shouldShowSurfaceTextTabs,
  type ShellSurface,
} from "@altai/agent-ui";
