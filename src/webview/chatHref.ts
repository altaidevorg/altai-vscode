/**
 * Pure chat markdown href helpers.
 * Shared implementation lives in `@altai/agent-ui` (Wave 4 / A6.13).
 */

export {
  hrefToFilePath,
  isWebHref,
  resolveWorkspacePath,
} from "@altai/agent-ui";
