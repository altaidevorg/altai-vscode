/**
 * Pure helpers for plan-mode strip and sticky todo summary chrome.
 * Shared implementation lives in `@altai/agent-ui` (A6.93).
 */

export {
  isPlanPermissionMode,
  permissionModeAfterExitPlan,
  latestTodoItemsFromDisplayMessages as latestTodosFromMessages,
} from "@altai/agent-ui";
