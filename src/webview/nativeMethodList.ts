/**
 * Native RPC method list parse/availability.
 * Shared implementation lives in `@altai/agent-ui` (A6.141).
 */

export {
  parseNativeMethodList,
  nativeMethodAvailable,
} from "@altai/agent-ui";
