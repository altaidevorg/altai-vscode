/**
 * Pure helpers for Operations → Chat deep-links (no React / vscode APIs).
 * Shared implementation lives in `@altai/agent-ui` (A6.100).
 */

export {
  buildOpenChatFocus,
  chatFocusStatusLine,
  type OpenChatFocus,
  type OpenChatTarget,
} from "@altai/agent-ui";
