/**
 * Pure helpers for skills status chrome (list-only; no install UI).
 * Shared implementation lives in `@altai/agent-ui` (A6.79).
 */

export {
  canMountSkillsStatus,
  skillsSummaryCopy,
  sortSkillsForDisplay,
  type SkillView,
} from "@altai/agent-ui";
