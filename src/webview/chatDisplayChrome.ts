/**
 * VS Code re-exports flat display-transcript list + role chrome from
 * `@altai/agent-ui` (A6.38). Host still owns bubble body and open-file actions.
 */

export {
  AiDisplayTranscriptList,
  chatDisplayBubbleClassName,
  chatDisplayBubbleModifier,
  chatDisplayRoleLabel,
  type AiDisplayTranscriptListProps,
} from "@altai/agent-ui";
