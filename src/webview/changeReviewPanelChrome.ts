/**
 * Pure helpers for open/dismiss change-review panel (no Apply without host).
 * Shared implementation lives in `@altai/agent-ui` (A6.69).
 */

export {
  dismissAllChangeReviewIds,
  dismissChangeReviewId,
  listChangeReviewItems,
  planLineDiffStats,
  type ChangeReviewItem,
  type ChangeReviewSourceMessage,
} from "@altai/agent-ui";
