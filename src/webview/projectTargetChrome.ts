/**
 * Pure helpers for the Chat project-target chip.
 * Shared implementation lives in `@altai/agent-ui` (A6.86).
 */

export {
  basenamePath,
  canMountProjectTarget,
  projectTargetFromWorkspace,
  retainPreferredRoot,
  type ProjectTargetView,
  type WorkspaceTargetInfo,
} from "@altai/agent-ui";
